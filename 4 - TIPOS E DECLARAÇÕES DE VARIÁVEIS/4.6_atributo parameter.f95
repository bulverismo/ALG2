program atr_parameter
        integer, parameter :: m=100, n=20, &
                nz = m*n - m
        real :: PI
        parameter (PI = 3.1415926)

        print *,pi

        ! parameter é utilizado quando o valor da variável não será
        ! modificado

end program atr_parameter
