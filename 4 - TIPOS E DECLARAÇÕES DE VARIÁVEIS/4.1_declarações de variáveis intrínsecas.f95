program declaracao
        !       segue abaixo série de formas de se declarar variáveis
        real :: X, Y, Z=-1.0
        real A, B, C
        real*8 :: U=3.0 ! uma variável REAL 
                        ! com 8 bytes de
                        ! tamanho
        double precision :: M=100.0D0, N
        double precision Q, P, O
        integer :: i=4, j, k
        integer f1, f2
        integer(2) :: DOIS      ! Uma variável
                                ! integer com 2
                                ! bytes de tamanho
        complex :: rho = (0.0,3.0)
        complex R, V, W
        character :: simbolo = ' '
        character(len=10) :: nome = 'JOSE SILVA'
        character*13 :: nome2 = 'manoel carlos'
        logical :: achou = .FALSE.
        logical procure, fim

end program declaracao 
